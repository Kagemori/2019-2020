#include <stdio.h>

double max(double a, double b){
	if(x>=y) return x;
	else return y;
}

int main(int argc, char ** argv){
	double a = 4;
	double b = 7;

	printf("The max of %g and %g is %g\n",a,b,max(a,b));
}
